/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Note: API route config (bodyParser, responseLimit) is handled in each route file
  // using the `config` export, not here in next.config.js
}

module.exports = nextConfig

